<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ExpenseApp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ExpenseApp">

        <!-- 🔥 MAIN -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OTHER SCREENS -->
        <activity android:name=".RegisterActivity" android:exported="false"/>
        <activity android:name=".HomeActivity" android:exported="false"/>
        <activity android:name=".ChartActivity" android:exported="false"/>
        <activity android:name=".WalletActivity" android:exported="false"/>
        <activity android:name=".AlertActivity" android:exported="false"/>

        <!-- 🔥 FIX VĂNG APP (QUAN TRỌNG) -->
        <activity android:name=".EditActivity" android:exported="false"/>

    </application>

</manifest>